# HypeMarket app (Next.js)

The hackathon application lives in this directory. See the **[root README](../README.md)** for architecture, environment setup, load testing, and demo instructions.

## Quick start

```bash
npm install
cp ../infrastructure/dsql/cluster.env.example .env.local
# Fill in AWS + IoT vars — see root README env table
npm run dev
```

Open http://localhost:3000
